- JOB: "intercept -g $DEVNODE | dual-function-keys -c /etc/interception/SUPERKEY_LAUNCHER.yaml | uinput -d $DEVNODE"
  DEVICE:
    NAME: "AT Translated Set 2 keyboard"
- JOB: "intercept -g $DEVNODE | dual-function-keys -c /etc/interception/SUPERKEY_LAUNCHER.yaml | uinput -d $DEVNODE"
  DEVICE:
    NAME: "Gaming KB  Gaming KB "
- JOB: "intercept -g $DEVNODE | dual-function-keys -c /etc/interception/SUPERKEY_LAUNCHER.yaml | uinput -d $DEVNODE"
  DEVICE:
    NAME: "Compx 2.4G Wireless Receiver"
- JOB: "intercept -g $DEVNODE | dual-function-keys -c /etc/interception/SUPERKEY_LAUNCHER.yaml | uinput -d $DEVNODE"
  DEVICE:
    NAME: "Keychron Keychron Q1 Keyboard"
- JOB: "intercept -g $DEVNODE | dual-function-keys -c /etc/interception/SUPERKEY_LAUNCHER.yaml | uinput -d $DEVNODE"
  DEVICE:
    NAME: ".*Qwertykeys.*"
